<?xml version="1.0" encoding="UTF-8" ?>
<class name="PLATEAUCityModel" inherits="RefCounted" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/godotengine/godot/master/doc/class.xsd">
	<brief_description>
		Main class for loading and processing CityGML files.
	</brief_description>
	<description>
		PLATEAUCityModel is the primary class for loading PLATEAU CityGML files and extracting mesh data. It handles parsing of CityGML format, texture loading, and conversion to Godot-compatible meshes.
		[codeblock]
		var city_model = PLATEAUCityModel.new()
		if city_model.load("path/to/file.gml"):
		    var options = PLATEAUMeshExtractOptions.new()
		    options.coordinate_zone_id = 9  # Tokyo
		    var meshes = city_model.extract_meshes(options)
		[/codeblock]
	</description>
	<tutorials>
	</tutorials>
	<methods>
		<method name="load">
			<return type="bool" />
			<param index="0" name="gml_path" type="String" />
			<description>
				Load a CityGML file from the specified path. Returns [code]true[/code] if loading was successful.
				[b]Note:[/b] This method is not supported on mobile platforms (Android/iOS). Use pre-converted assets instead.
			</description>
		</method>
		<method name="is_loaded" qualifiers="const">
			<return type="bool" />
			<description>
				Returns [code]true[/code] if a CityGML file has been successfully loaded.
			</description>
		</method>
		<method name="get_gml_path" qualifiers="const">
			<return type="String" />
			<description>
				Returns the path of the currently loaded GML file.
			</description>
		</method>
		<method name="extract_meshes">
			<return type="PLATEAUMeshData[]" />
			<param index="0" name="options" type="PLATEAUMeshExtractOptions" />
			<description>
				Extract meshes from the loaded CityGML using the specified options. Returns an array of [PLATEAUMeshData] objects.
				[b]Note:[/b] This method is not supported on mobile platforms (Android/iOS). Use pre-converted assets instead.
			</description>
		</method>
		<method name="get_center_point" qualifiers="const">
			<return type="Vector3" />
			<param index="0" name="coordinate_zone_id" type="int" />
			<description>
				Get the center point of the city model in the specified coordinate zone.
			</description>
		</method>
		<method name="get_city_object_attributes" qualifiers="const">
			<return type="Dictionary" />
			<param index="0" name="gml_id" type="String" />
			<description>
				Get attributes of a CityObject by its GML ID. Returns an empty Dictionary if not found.
			</description>
		</method>
		<method name="get_city_object_type" qualifiers="const">
			<return type="int" />
			<param index="0" name="gml_id" type="String" />
			<description>
				Get the CityObject type by GML ID. Returns a [enum PLATEAUCityObjectType] value.
			</description>
		</method>
		<method name="load_async">
			<return type="void" />
			<param index="0" name="gml_path" type="String" />
			<description>
				Load a CityGML file asynchronously in background thread. Emits [signal load_completed] when done.
				Use [method is_processing] to check if loading is in progress.
				[codeblock]
				city_model.load_completed.connect(_on_load_completed)
				city_model.load_async("path/to/file.gml")

				func _on_load_completed(success: bool):
				    if success:
				        city_model.extract_meshes_async(options)
				[/codeblock]
			</description>
		</method>
		<method name="extract_meshes_async">
			<return type="void" />
			<param index="0" name="options" type="PLATEAUMeshExtractOptions" />
			<description>
				Extract meshes asynchronously in background thread. Emits [signal extract_completed] when done.
				The model must be loaded first using [method load] or [method load_async].
				[codeblock]
				city_model.extract_completed.connect(_on_extract_completed)
				city_model.extract_meshes_async(options)

				func _on_extract_completed(meshes: Array):
				    for mesh_data in meshes:
				        # Process mesh data
				        pass
				[/codeblock]
			</description>
		</method>
		<method name="is_processing" qualifiers="const">
			<return type="bool" />
			<description>
				Returns [code]true[/code] if an async load or extract operation is in progress.
			</description>
		</method>
	</methods>
	<signals>
		<signal name="load_completed">
			<param index="0" name="success" type="bool" />
			<description>
				Emitted when [method load_async] completes. [param success] is [code]true[/code] if the file was loaded successfully.
			</description>
		</signal>
		<signal name="extract_completed">
			<param index="0" name="mesh_data_array" type="Array" />
			<description>
				Emitted when [method extract_meshes_async] completes. [param mesh_data_array] contains the extracted [PLATEAUMeshData] objects.
			</description>
		</signal>
	</signals>
	<members>
		<member name="log_level" type="int" setter="set_log_level" getter="get_log_level" default="2">
			Controls the verbosity of CityGML parser log messages. Use [constant LOG_LEVEL_NONE] to suppress all messages, or [constant LOG_LEVEL_WARNING] (default) for normal operation.
			This is useful when loading downloaded GML files where missing texture/codelist warnings are expected and can be safely ignored.
			[codeblock]
			var city_model = PLATEAUCityModel.new()
			city_model.log_level = PLATEAUCityModel.LOG_LEVEL_NONE  # Suppress all logs
			city_model.load("path/to/downloaded.gml")
			[/codeblock]
		</member>
	</members>
	<constants>
		<constant name="LOG_LEVEL_NONE" value="0">
			No log messages. Completely silent operation.
		</constant>
		<constant name="LOG_LEVEL_ERROR" value="1">
			Only error messages.
		</constant>
		<constant name="LOG_LEVEL_WARNING" value="2">
			Warnings and errors. This is the default level.
		</constant>
		<constant name="LOG_LEVEL_INFO" value="3">
			Info, warnings, and errors.
		</constant>
		<constant name="LOG_LEVEL_DEBUG" value="4">
			All messages including debug output.
		</constant>
	</constants>
</class>
